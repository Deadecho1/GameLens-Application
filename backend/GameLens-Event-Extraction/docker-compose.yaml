services:
  event_classifier:
    build: .
    env_file:
      - .env
    ports:
      - 7761:7761
    networks:
      - db_network
    environment:
      - MALLOC_TRIM_THRESHOLD_=100000

networks:
  db_network:
    external: true
